[project]
name = "luna-ai"
version = "0.0.5"
description = "Luna AI — SMS-native agentic travel assistant"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
