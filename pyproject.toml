[project]
name = "luna-ai"
version = "0.0.1"
description = "Luna AI — SMS-native agentic travel assistant"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
